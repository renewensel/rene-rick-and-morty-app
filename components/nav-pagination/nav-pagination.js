import { createCharacterCard } from "../card/card.js";
import { cardContainer } from "../../index.js";

export function prevNextPagination(customURL) {
  console.log("Hello fron nav-pagination.js");
  console.log("customURL: ", customURL);
  //   cardContainer.insertAdjacentHTML(
  //     "beforeend",
  //     createCharacterCard(
  //       character.image,
  //       character.name,
  //       character.type,
  //       character.status,
  //       character.episode.length
  //     )
  //   );
}
